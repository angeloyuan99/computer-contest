#include<iostream>
#include<stack>
using namespace std;
int main(){
    string input;
    cin >> input;
    stack<pair<string, int> > nobrackets;
    stack<pair<string, int> > order;
    for(int i = 0; i < input.length(); i++){
        order.push(n)
    }
}